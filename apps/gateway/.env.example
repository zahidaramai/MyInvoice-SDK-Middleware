# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info

# Session configuration
SESSION_TTL_MS=86400000
TOKEN_RENEW_SKEW_MS=30000

# Upstream validation (set to true to validate credentials on session creation)
VALIDATE_UPSTREAM=false
